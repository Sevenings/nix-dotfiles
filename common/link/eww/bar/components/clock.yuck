(defpoll time :interval "1s"
  `date +%H:%M`)
(defpoll date :interval "1s"
  `date "+%d %B %Y"`)

(defwidget clock []
  (eventbox :onclick "openCalendar"
    (label :class "clock"
           :halign "center"
           :space-evenly false
           :text " ${date}   ${time}"
    )
  )
)
